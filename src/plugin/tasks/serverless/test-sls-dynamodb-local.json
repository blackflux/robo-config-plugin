{
  "target": "test/1-sls-dynamodb-local.spec.js",
  "format": "txt",
  "strategy": "overwrite",
  "snippets": [
    {
      "name": "serverless-dynamodb/test/1-sls-dynamodb-local.spec.js",
      "variables": {
        "awsRegion": "${awsRegion}"
      }
    }
  ],
  "requires": [
    "serverless",
    "aws"
  ],
  "purpose": [
    "Test for generating ./docker/dynamodb.sh script for DynamoDB table(s) creation."
  ]
}
