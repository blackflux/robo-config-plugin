{
  "target": "serverless/s3/api/${bucket}.yml",
  "format": "txt",
  "strategy": "overwrite",
  "snippets": [
    {
      "name": "serverless/s3/api/${bucket}.yml",
      "variables": {
        "bucket": "${bucket|TITLE}"
      }
    }
  ],
  "requires": [
    "serverless",
    "aws"
  ],
  "purpose": [
    "Initialize api stack resource definitions for an s3 bucket."
  ]
}
