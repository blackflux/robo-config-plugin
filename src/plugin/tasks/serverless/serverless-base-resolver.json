{
  "target": "serverless/.base/resolver.yml",
  "format": "txt",
  "strategy": "overwrite",
  "snippets": [
    {
      "name": "serverless/.base/resolver.yml",
      "variables": {
        "projectName": "${projectName}",
        "namespace": "${namespace}"
      }
    }
  ],
  "requires": [
    "serverless",
    "aws"
  ],
  "purpose": [
    "Create serverless stack resolver configuration file."
  ]
}
