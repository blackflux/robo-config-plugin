{
  "target": "serverless/api.yml",
  "format": "yml",
  "strategy": "merge-deep",
  "snippets": [
    {
      "name": "serverless/crons/api.yml",
      "variables": {
        "cron": "${cron|LOWER}"
      }
    }
  ],
  "requires": [
    "serverless",
    "aws"
  ],
  "purpose": [
    "Reference api stack resource definitions for an api cron."
  ]
}
