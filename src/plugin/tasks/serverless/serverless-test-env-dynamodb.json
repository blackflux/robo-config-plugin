{
  "target": "test/env-vars.yml",
  "format": "yml",
  "strategy": "merge-deep",
  "snippets": [
    {
      "name": "serverless/dynamodb/env-vars.yml",
      "variables": {
        "table": "${table}",
        "projectName": "${projectName}"
      }
    }
  ],
  "requires": [
    "serverless",
    "aws"
  ],
  "purpose": [
    "Initialize DynamoDb Table Name in test configuration."
  ]
}
