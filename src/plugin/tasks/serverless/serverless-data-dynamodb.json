{
  "target": "serverless/data.yml",
  "format": "yml",
  "strategy": "merge-deep",
  "snippets": [
    {
      "name": "serverless/dynamodb/data.yml",
      "variables": {
        "table": "${table|LOWER}"
      }
    }
  ],
  "requires": [
    "serverless",
    "aws"
  ],
  "purpose": [
    "Reference data stack resource definitions for a dynamodb table."
  ]
}
