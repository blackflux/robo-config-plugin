{
  "target": "test/env-vars.yml",
  "format": "yml",
  "strategy": "merge-deep",
  "snippets": [
    {
      "name": "serverless/s3/env-vars.yml",
      "variables": {
        "bucket": "${bucket}",
        "projectName": "${projectName}"
      }
    }
  ],
  "requires": [
    "serverless",
    "aws"
  ],
  "purpose": [
    "Initialize S3 Name in test configuration."
  ]
}
