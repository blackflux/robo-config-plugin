{
  "target": "src/handler/queue/processor/${queue}.js",
  "format": "txt",
  "strategy": "overwrite",
  "snippets": [
    {
      "name": "serverless/sqs/${queue}.js",
      "variables": {
        "queue": "${queue|CAMEL}"
      }
    }
  ],
  "requires": [
    "serverless",
    "aws"
  ],
  "purpose": [
    "Create queue processor handler."
  ]
}
