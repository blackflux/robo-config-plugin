{
  "target": "docker/Dockerfile",
  "strategy": "overwrite",
  "snippets": [
    "sls-react/Dockerfile"
  ],
  "requires": [
    "docker",
    "serverless"
  ],
  "purpose": [
    "Docker environment definition for running tests in react AWS Lambda container."
  ]
}
