{
  "target": "docker/Dockerfile",
  "strategy": "overwrite",
  "snippets": [
    "es/Dockerfile"
  ],
  "requires": [
    "docker"
  ],
  "purpose": [
    "Docker environment definition for running tests in AWS Lambda container."
  ]
}
