{
  "target": "babel.config.json",
  "format": "json",
  "strategy": "merge-deep",
  "snippets": [
    {
      "name": "default/babel.config.json",
      "variables": {
        "nodeVersion": "${nodeVersion}"
      }
    }
  ],
  "requires": [
    "javascript",
    "babel"
  ],
  "purpose": [
    "Create configuration file for babel."
  ]
}
