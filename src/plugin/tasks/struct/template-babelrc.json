{
  "target": ".babelrc",
  "format": "json",
  "strategy": "merge-deep",
  "snippets": [
    {
      "name": "default/.babelrc",
      "variables": {
        "nodeVersion": "${nodeVersion}"
      }
    }
  ],
  "requires": [
    "javascript",
    "babel"
  ],
  "purpose": [
    "Create configuration file for babel."
  ]
}
