{
  "target": "test/env.recording.yml",
  "format": "txt",
  "strategy": "create-only",
  "snippets": [
    {
      "name": "sls-api/test/env.recording.yml",
      "variables": {
        "awsRegion": "${awsRegion}"
      }
    }
  ],
  "requires": [
    "lambda-tdd"
  ],
  "purpose": [
    "Create recording configuration template for lambda-tdd test setup."
  ]
}
