{
  "target": "package.json",
  "pretty": false,
  "strategy": "merge-deep",
  "snippets": [
    {
      "name": "default/merge-deep",
      "variables": {
        "repoKey": "${repoKey}",
        "nodeVersion": "${nodeVersion}"
      }
    },
    {
      "name": "serverless/merge-deep",
      "variables": {
        "projectName": "${projectName}"
      }
    }
  ],
  "requires": [
    "javascript"
  ],
  "purpose": [
    "Set various entries."
  ]
}
