{
  "target": "package.json",
  "pretty": false,
  "strategy": "merge-shallow",
  "snippets": [
    {
      "name": "serverless-react/merge-shallow.json.mustache",
      "variables": {
        "awsRegion": "${awsRegion}",
        "nccEnabled": "${nccEnabled}",
        "nccExclude": "${nccExclude}"
      }
    }
  ],
  "requires": [
    "javascript"
  ],
  "purpose": [
    "Set various entries."
  ]
}
