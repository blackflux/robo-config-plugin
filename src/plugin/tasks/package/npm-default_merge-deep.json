{
  "target": "package.json",
  "strategy": "merge-deep",
  "snippets": [
    {
      "name": "default/merge-deep",
      "variables": {
        "repoKey": "${repoKey}"
      }
    },
    {
      "name": "npm-default/merge-deep",
      "variables": {
        "packageName": "${packageName}"
      }
    }
  ],
  "requires": [
    "javascript"
  ],
  "purpose": [
    "Set various entries."
  ]
}
