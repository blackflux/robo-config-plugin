{
  "target": "package.json",
  "pretty": false,
  "strategy": "merge-deep",
  "snippets": [
    {
      "name": "default/merge-deep",
      "variables": {
        "repoKey": "${repoKey}",
        "nodeVersion": "${nodeVersion}"
      }
    },
    {
      "name": "npm-default/merge-deep",
      "variables": {
        "packageName": "${packageName}"
      }
    }
  ],
  "requires": [
    "javascript"
  ],
  "purpose": [
    "Set various entries."
  ]
}
