{
  "target": "package.json",
  "strategy": "merge-deep",
  "snippets": [
    {
      "name": "default/merge-deep",
      "variables": {
        "repoKey": "${repoKey}"
      }
    },
    {
      "name": "serverless-react/merge-deep",
      "variables": {
        "projectName": "${projectName}"
      }
    }
  ],
  "requires": [
    "javascript"
  ],
  "purpose": [
    "Set various entries."
  ]
}
