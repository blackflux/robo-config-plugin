{
  "target": "package.json",
  "pretty": false,
  "strategy": "merge-shallow",
  "snippets": [
    {
      "name": "npm-default/merge-shallow.json.mustache",
      "variables": {
        "nccEnabled": "${nccEnabled}"
      }
    },
    {
      "name": "serverless-dynamodb/merge-shallow",
      "variables": {
        "awsRegion": "${awsRegion}",
        "awsAccessKeyId": "${awsAccessKeyId}",
        "awsSecretAccessKey": "${awsSecretAccessKey}"
      }
    }
  ],
  "requires": [
    "javascript"
  ],
  "purpose": [
    "Set various entries."
  ]
}
