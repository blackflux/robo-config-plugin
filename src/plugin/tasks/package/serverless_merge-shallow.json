{
  "target": "package.json",
  "pretty": false,
  "strategy": "merge-shallow",
  "snippets": [
    {
      "name": "serverless/merge-shallow.json.mustache",
      "variables": {
        "nccEnabled": "${nccEnabled}",
        "nccExclude": "${nccExclude}"
      }
    }
  ],
  "requires": [
    "javascript"
  ],
  "purpose": [
    "Set various entries."
  ]
}
