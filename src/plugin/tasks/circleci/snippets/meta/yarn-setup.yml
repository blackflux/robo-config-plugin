steps:
  - checkout
  - run:
      command: |
        if [[ -z "$\{NPM_TOKEN}" ]]; then
          echo "//registry.npmjs.org/:_authToken=$\{NPM_TOKEN}" > ~/.npmrc
        fi
  - run: yarn install --frozen-lockfile
